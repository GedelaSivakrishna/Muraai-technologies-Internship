/* Types of equality operators in js 
    1. ==
     -> It does type conversion if both values are of not same type else
        performs === on the operands.
    2. ===
     -> It returns false if types of operands are not same else 
        compares values.
*/

// console.log(1 == '1');
// console.log(1 === '1');
// console.log(0 == -0);
// console.log(NaN == NaN);