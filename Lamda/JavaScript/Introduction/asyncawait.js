/* 
    async await -> are a better way to handle asynchronous code (which doesn't return value at the same time)
    1. async
        function which marked as async is always going to return promise.
    2. await
        await makes the code execute like a synchronous code

    Note- They don't provide any special features but a better & clean way to handle async code in js
*/